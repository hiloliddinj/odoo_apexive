import 'dart:ui';

/// App colors
class AppColors {
  /// App light color
  static const appColorLight = Color(0xFF214ECC);

  /// App dark color
  static const appColorDark = Color(0xFF0C1D4D);

  /// cardColorLight
  static const cardColorLight = Color.fromRGBO(255, 255, 255, 0.1);
}
